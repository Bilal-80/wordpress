apiVersion: v1
kind: PersistantVolume
metadata:
  name: mysql-pv
spec:
  capacity: 
    storage: 2Gi
    accessModes:
      - ReadWriteOnce
    hostPath:
      path: /var/lib/mysql
    persistantVolumeClaimPolicy: Retain
    